{
  "name": "@mtgatricks/desktop",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && mv dist/preload.js dist/preload.mjs",
    "start": "electron .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@mtgatricks/arena": "workspace:*",
    "@mtgatricks/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "electron": "^38.0.0",
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
