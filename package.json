{
  "name": "mtgatricks",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "start": "pnpm --filter @mtgatricks/desktop start",
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0"
  }
}
